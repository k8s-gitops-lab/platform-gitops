# ServiceAccount utilise par le ClusterSecretStore (provider kubernetes)
# pour lire les secrets sources dans le namespace argocd.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secrets-distribution
  namespace: external-secrets
