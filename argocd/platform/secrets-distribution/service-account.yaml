# ServiceAccount utilise par les ClusterSecretStores (provider kubernetes)
# pour lire les secrets sources dans les namespaces argocd et gitlab.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secrets-distribution
  namespace: external-secrets
